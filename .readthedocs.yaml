# ========================================================================
# .readthedocs.yaml
# ========================================================================
# ReadTheDocs documentation for the Python bindings

version: "2"

build:
  os: "ubuntu-22.04"
  tools:
    python: "3.14"

sphinx:
  configuration: forj-python/docs/conf.py
  builder: "dirhtml"

python:
  install:
    - requirements: forj-python/docs/requirements.txt